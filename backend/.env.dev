# ─────────────────────────────────────────────────────────────
#  Dev / mock environment — no PostgreSQL or Redis required
#  Uses SQLite on-disk file + fakeredis (in-process)
# ─────────────────────────────────────────────────────────────

DATABASE_URL=sqlite+aiosqlite:///./dev_mock.db
REDIS_URL=redis://localhost:6379/0    # not used; fakeredis is active
DEV_FAKE_REDIS=1

SECRET_KEY=f06601905b779fbc42bf8ba11cb7610cd77e582656a501052bf2710bdd999bdd
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

BACKEND_CORS_ORIGINS=["http://localhost:5173"]
